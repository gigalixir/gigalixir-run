# this is the default Procfile used if the user does not specify one
# TODO: custom Procfiles that do not use $GIGALIXIR_COMMAND won't be able to hot upgrade
# or remote_console?
# use Procfile for init only?
# TODO: honcho logging is very slow/delayed. why?
web: /app/bin/$GIGALIXIR_APP_NAME $GIGALIXIR_COMMAND
